<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.bistelapp.bistel.riders.VoucherActivity">

    <TextView
        android:id="@+id/voucher"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Enter voucher code"
        android:textColor="#000"
        android:textSize="20sp" />

    <EditText
        android:id="@+id/voucher_code"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/voucher"
        android:layout_marginTop="5dp"/>

    <Button
        android:id="@+id/verify"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/voucher_code"
        android:background="#000"
        android:textColor="#fff"
        android:text="verify"
        android:layout_marginTop="5dp"/>

    <TextView
        android:id="@+id/notification"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/verify"
        android:layout_marginTop="10dp"
        android:background="@color/myDrawerBackground"
        android:text="*Use previous voucher before inputting a new one."
        android:textColor="@color/myAccentColor"
        android:textSize="12sp" />

</RelativeLayout>
